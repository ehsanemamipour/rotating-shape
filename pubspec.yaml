name: rotating_shape
description: A Rotating shape widget.
version: 0.0.1
homepage: https://github.com/ehsanemamipour/rotating_shape

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
