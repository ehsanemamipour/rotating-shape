name: rotating_shape
description: Introducing the RotatingShape widget, the perfect solution for animating polygons and other shapes in your Flutter app.
version: 0.0.1
homepage: https://github.com/ehsanemamipour/rotating_shape

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
